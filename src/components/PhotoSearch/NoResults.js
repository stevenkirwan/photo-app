/**
 * No Results component 
 * Handles the rendering of No Results
 * if the API returns nothing show this component
 */

import React from "react";

const NoResults = () => <p>No results. Try again....</p>;

export default NoResults;